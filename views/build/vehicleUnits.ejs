<%- layout("layout/boilerplate") %> 
<div class="container">
    <div class="row justify-content-around">
        <div class="col-10">
            <table class="table table-striped table-hover">
                <thead>
                    <tr class="tableHead">
                        <th scope="col">Name</th>
                        <th scope="col" colspan="2">Price</th>
                        <th scope="col">Att/Life</th>
                        <th scope="col">Targets</th> 
                        <th scope="col">Owned</th> 
                        <th scope="col">Build</th> 
                    </tr>
                </thead>
                <form action="/vehicle_units" method="POST">
                <tbody>
                    <% for (let key of Object.keys(vehUnits)){ %> 
                        <tr>
                            <th scope="row"><%=vehUnits[key].name %></th>
                            <td colspan="2"><%=`$${vehUnits[key].price}`%></td>
                            <td colspan=""><%=`${vehUnits[key].attack}/${vehUnits[key].defense}`%></td>
                            <td colspan=""><%=vehUnits[key].targets%></td>
                            <td colspan=""><%=vehUnits[key].amount%></td>
                            <td><input class="unitInput" type="text" name="<%=key%>[amount]"></td>
                        </tr>
                        <% } %> 
                </tbody>
              </table>
              <div class="d-grid">
                <button type="submit" class="btn btn-secondary">Add Units</button>
            </div>
        </form>
        </div>
    </div>
</div>